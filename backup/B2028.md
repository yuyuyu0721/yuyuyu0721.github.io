# 反向输出一个三位数
## 题目简单重在了解字符串反转
```python
n = input().strip()
print(n[::-1])
```
>[!NOTE]
其中n[::-1]即为字符串反转重点
运用了切片一特性
>

>[!TIP]
切片是从序列（如字符串、列表、元组等）中提取部分元素
格式为：对象[start:end:step] start为开始索引（默认为0） end为结束索引 step为步长（默认为1）
>
# 总结
主要是学习字符串反转和切片的定义和格式

